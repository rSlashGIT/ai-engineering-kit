# **PROJECT_NAME** -- Agent Protocol

This file is the single source of truth for any AI agent interacting with this project.

## Source

This project was scaffolded from the [AI Engineering Kit](https://github.com/your-org/ai-engineering-kit).

## Required Reading

Before any task, read these in order:

1. `README.md` -- project overview
2. `AGENTS.md` -- this file
3. Relevant standards (refer to kit)

## Workflow

1. Read context and existing code.
2. Plan before writing code.
3. Implement matching project conventions.
4. Run `npm test` and `npm run lint` before finishing.
5. Iterate on feedback.

## Constraints

- Never commit secrets or credentials.
- Never add stubs or fake implementations unless explicitly asked.
- Run the test suite before and after changes.
- Update docs when behaviour changes.
- Follow strict TypeScript -- all functions must have type annotations.
- Use ESLint + Prettier for linting and formatting -- run `npm run lint` and `npm run format`.
