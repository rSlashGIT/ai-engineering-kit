# templates — Project Scaffolds

**Purpose:** Cookie-cutter project skeletons for bootstrapping new repositories. The `generic/` template is language-agnostic; language-specific variants add linting, build, and package configs.
