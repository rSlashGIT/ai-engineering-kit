# Coding Standard — TypeScript

**Purpose:** TypeScript-specific coding conventions extending the general standard. Covers formatting, types, imports, async patterns, and tooling (biome, vitest, tsc).

**Expected contents:**
- Tooling recommendations (biome, vitest, tsc)
- Strict mode configuration requirements
- Type definition and declaration conventions
- Async/await and Promise patterns
- Module and import organisation
- Testing conventions (vitest, mocks, coverage)
- Build and bundler configuration
