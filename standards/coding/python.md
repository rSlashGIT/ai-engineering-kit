# Coding Standard — Python

**Purpose:** Python-specific coding conventions extending the general standard. Covers formatting, imports, type hints, docstrings, and tooling (ruff, mypy, pytest).

**Expected contents:**
- Tooling recommendations (ruff, mypy, pytest, pre-commit)
- Import ordering and organisation rules
- Type hint requirements (strict mypy mode)
- Docstring conventions (Google or NumPy style)
- Testing conventions (pytest, fixtures, parametrisation)
- Async/await patterns and best practices
- Dependency management (pip, poetry, uv)
