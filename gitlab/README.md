# gitlab — GitLab Configuration

**Purpose:** GitLab Duo and Fable review workflow configurations for projects that mirror to GitLab. Mirrors the conventions in `.github/` for the GitLab ecosystem.

**Expected contents:**
- GitLab CI/CD pipeline definitions
- GitLab Duo AI review configurations
- Fable code review workflow definitions
- Merge request templates
- GitLab-specific lint and test job definitions
